#!/bin/zsh
# T_BEGIN=$(ruby -e 'puts Time.now.to_f')
. $HOME/.homesick/repos/rc-files/zsh/rc.zsh
# T_END=$(ruby -e 'puts Time.now.to_f')
# STARTUP_TIME=$(($T_END-$T_BEGIN))
# printf "Startup time: %f s\n" $STARTUP_TIME
